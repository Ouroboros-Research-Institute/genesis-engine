# Genesis Engine — core dependencies
# Python 3.10+ recommended (tested on 3.13)

numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0

# 2D mesh / spherical geometry
# (mesh_utils.py builds icospheres + cotangent Laplace-Beltrami in pure numpy/scipy;
#  no trimesh/igl required at runtime)

# Optional: figure capture (only needed to regenerate Fig 6 from the live dashboard)
# playwright>=1.40
# Pillow>=10.0

# Optional: PDF verification during paper builds
# pymupdf>=1.23
